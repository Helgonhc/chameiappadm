/** @type {import('next').NextConfig} */
const nextConfig = {
  distDir: '.next_dev',
  images: {
    unoptimized: true,
  },
  eslint: {
    ignoreDuringBuilds: true,
  },
  typescript: {
    ignoreBuildErrors: true,
  },
};

module.exports = nextConfig;
